# 前端部署流程入門筆記

> **這份筆記的目標**：你已經會寫 Angular，但你可能不清楚 `ng build` 完成後，那些檔案是什麼、為什麼需要 Nginx、以及 Docker 裡三個檔案各自在幹嘛。
>
> **閱讀順序**：建議從頭讀到尾。每一章都建立在上一章的認知上。

---

## 第一章：Angular 不是 Node.js 應用程式

這是一切的前提。很多人誤以為 Angular 是「Node.js 應用程式」，所以伺服器上要跑 Node.js。**這是錯的。**

### 1.1 開發時：你在寫程式，需要 Node.js

開發時，你的本機上有：

- `src/app/dashboard/dashboard.component.ts` 等原始碼
- `node_modules/`（幾千個依賴套件）
- Angular CLI（`ng` 指令）
- `ng serve` 開發伺服器

你存檔，瀏覽器自動刷新。這個過程**需要 Node.js**，因為：

- TypeScript 要編譯成 JavaScript
- 檔案變動要偵測、重新編譯
- 開發伺服器要處理路由、模組熱更新

這時候 Node.js 是「執行環境」，讓你的開發工具能動起來。

### 1.2 Build：Node.js 變成編譯工具

執行 `ng build`，Angular CLI 做一堆事：

1. 把 TypeScript 編譯成 JavaScript
2. 把 Component template 編譯成可執行碼
3. 把 CSS 壓縮
4. 在 JS/CSS 檔名加 content hash（`main.a3f2b1c.js`）
5. 產生最終的 `index.html`

最後產出的是：

```
dist/my-angular-app/browser/
├── index.html          ← 純 HTML
├── main.a3f2b1c.js     ← 純 JavaScript
├── styles.d4e5f6.css   ← 純 CSS
└── ...
```

Build 階段需要 Node.js，因為編譯器（Angular CLI）本身就是 Node.js 寫的程式。但這時候 Node.js 的角色是「編譯工具」，不是「伺服器」。

### 1.3 Runtime：瀏覽器自己執行，不需要 Node.js

這些檔案已經不需要 Node.js 了。它們和你在網路上看到的任何網頁沒有區別——就是 HTML、CSS、JavaScript 檔案。

部署後的流程：

```
使用者瀏覽器 ──HTTP──▶ 某個 HTTP server
                              │
                              ▼
                    回傳 index.html + main.js + styles.css
                              │
                              ▼
                    瀏覽器下載後，自己的 JavaScript 引擎執行 Angular
```

| 角色 | 做什麼 |
|------|--------|
| **HTTP server** | 從硬碟讀檔案，透過 HTTP 傳給瀏覽器 |
| **瀏覽器** | 收到 `main.js` 後，自己執行裡面的 JavaScript（Angular 框架就在裡面） |

Node.js 完全不在這個流程裡。

### 1.4 類比

| 階段 | 比喻 | Node.js 在嗎？ |
|------|------|---------------|
| **開發** | 你在廚房做菜，需要瓦斯爐、冰箱、鍋子 | ✅ 需要（它是瓦斯爐） |
| **Build** | 把菜裝進便當盒，抽真空包裝 | ✅ 需要（它是包裝機） |
| **Runtime** | 客人打開便當盒直接吃，不需要廚房 | ❌ 不需要（便當盒自己就能吃） |

### 1.5 為什麼這個區分很重要

如果你理解這個區分，就會知道：

- Angular build 完成後，可以放到任何靜態 hosting（GitHub Pages、AWS S3、Netlify、傳統虛擬主機），全部都可以
- 這個專案用 Nginx，不是因為「Angular 需要 Nginx」，而是因為「靜態檔案需要一個 HTTP server」
- 用 Node.js 服務靜態檔案等同於開跑車去買菜——它會動，但不必要、不經濟、不高效

---

## 第二章：靜態檔案需要 HTTP server

### 2.1 `index.html` 不會自己變成網頁

`ng build` 產出的檔案只是**躺在硬碟上的檔案**。它們不會自動回應 HTTP 請求。

當瀏覽器打 `https://your-site.com` 時，發生的事是：

1. 瀏覽器送出 HTTP GET 請求
2. 某個 process 收到這個請求
3. 那個 process 去硬碟讀 `index.html`
4. 把那個檔案的內容包成 HTTP response 回傳

步驟 2~4 需要一個「HTTP server」來做。這可以是：

| HTTP server | 常見用途 |
|-------------|---------|
| Nginx | 高性能靜態檔案服務 + 反向代理 |
| Apache | 傳統靜態檔案服務 |
| Python `http.server` | 本地快速測試 |
| Node.js `express.static` | 開發或簡易部署 |
| Go 內建 `http.FileServer` | 極簡靜態服務 |

任何能「聽 HTTP port + 讀檔案 + 回傳 response」的程式都可以。

### 2.2 為什麼這個專案選 Nginx

如果只是「服務靜態檔案」，一個 20 行的 Go 程式就夠了。但這個專案的前端不只是純展示——它要呼叫後端 API。

這時候需要額外功能：

| 功能 | 為什麼需要 |
|------|-----------|
| **反向代理 API** | `/api/jobs` 要轉發到後端 `job-service`，不是回傳靜態檔案 |
| **CORS 處理** | 瀏覽器的同源政策限制，需要加 `Access-Control-Allow-Origin` header |
| **SPA fallback** | 前端路由（如 `/dashboard`）沒有對應檔案時，要回傳 `index.html` 讓 Angular Router 處理 |
| **Gzip 壓縮** | 減少傳輸量 |
| **快取控制** | JS/CSS 檔名有 hash 可以快取很久，HTML 不要快取 |

Nginx 一個工具同時解決這五件事，所以選它。

### 2.3 如果沒有 API 呼叫呢？

如果你的 Angular 專案只是純靜態展示（沒有 `/api/...` 呼叫），那麼：

- 可以考慮更簡單的 HTTP server（Go、Python、甚至 Node.js `http-server`）
- 甚至不需要 container——直接放 GitHub Pages、AWS S3、Cloud Storage 靜態網站代管

這個專案因為有 API 反向代理的需求，所以需要 Nginx 這種功能完整的 HTTP server。

---

## 第三章：部署到 Cloud Run 的特殊要求

### 3.1 Cloud Run 是什麼

這個專案的前端最終要部署到 **Google Cloud Run**。

Cloud Run 是 Google 提供的「容器代管平台」。它的運作方式：

1. 你給它一個 Docker image
2. 它啟動 container，把外部 HTTPS 請求轉發到 container 內部的某個 TCP port
3. 你 container 裡的 process 負責處理請求、回傳 response

### 3.2 Cloud Run 的容器合約

Cloud Run 對 container 有一個硬性要求：

> 你的 container 必須有一個 process 在 `0.0.0.0` 上監聽 `$PORT` 環境變數指定的 port（預設 8080），並且能處理 HTTP 請求。

這表示：

- Cloud Run **不會**幫你讀檔案
- Cloud Run **不會**幫你組 HTTP response
- Cloud Run **不會**幫你決定 `/api/jobs` 要打去哪

它只負責「把外部 HTTPS 請求送進你的 container」。進到 container 之後的一切，都是你的 HTTP server（Nginx）的責任。

### 3.3 為什麼這個認知重要

很多人第一次用 Cloud Run 會問：

> 「Cloud Run 是 serverless，應該會自動幫我 serve 靜態檔案吧？」

**不會。** Cloud Run 不是 Cloud Storage，不是 GitHub Pages，不是 Netlify。它不認得什麼是 `index.html`。它只認得一個在 port 上監聽的 HTTP server。

所以你的 container 裡「需要某個 HTTP server」。這個 HTTP server 可以是 Nginx、Apache、一個 Go 寫的 20 行小程式，或任何能聽 HTTP port 的東西。這個專案選的是 **Nginx**。

---

## 第四章：Docker 部署流程

這個專案用 Docker 把 Angular 打包成可以部署的容器。裡面有三個關鍵檔案，按照這個順序運作：

```
Dockerfile          ← 定義「容器裡有什麼、怎麼 build」
    │
    ▼
entrypoint.sh       ← 容器啟動時「準備設定檔、啟動 Nginx」
    │
    ▼
nginx.conf          ← Nginx 啟動後「決定每個請求怎麼處理」
```

### 4.1 Dockerfile：定義容器裡有什麼

```dockerfile
FROM node:18-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
RUN apk add --no-cache gettext
RUN rm -f /etc/nginx/conf.d/default.conf /etc/nginx/nginx.conf
COPY nginx.conf /etc/nginx/nginx.conf.template
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
COPY --from=build /app/dist/my-angular-app/browser /usr/share/nginx/html
ENV PORT=8080
EXPOSE 8080
CMD ["/entrypoint.sh"]
```

這是「多階段 build」（Multi-stage build），分兩段：

#### 第一段（build）：把原始碼變成靜態檔案

| 步驟 | 作用 |
|------|------|
| `FROM node:18-alpine AS build` | 用 Node.js image，這階段叫 `build` |
| `COPY package*.json` + `npm ci` | 安裝依賴 |
| `COPY . .` + `npm run build` | 把原始碼放進去，執行 Angular build |

跑完之後，`/app/dist/my-angular-app/browser/` 裡面就是 `index.html`、`main.xxx.js`、`styles.xxx.css` 等靜態檔案。

#### 第二段（runtime）：只留 Nginx 和靜態檔案

| 步驟 | 作用 |
|------|------|
| `FROM nginx:alpine` | 換成 Nginx image，**第一段的所有東西（Node.js、原始碼、node_modules）都丟掉** |
| `apk add gettext` | 安裝 `envsubst`，用來把環境變數塞進設定檔 |
| `rm -f /etc/nginx/...` | 刪掉 Nginx 預設設定 |
| `COPY nginx.conf ...template` | 把自訂設定放進去，但副檔名是 `.template`（還沒有環境變數） |
| `COPY entrypoint.sh` | 放啟動腳本 |
| `COPY --from=build ...` | 把第一段 build 好的靜態檔案，複製到 Nginx 預設的根目錄 |
| `CMD ["/entrypoint.sh"]` | 容器啟動時，執行這個腳本 |

> **關鍵認知**：`COPY --from=build` 只把靜態檔案帶過來，Node.js、原始碼、 `node_modules` 全部留在第一階段，build 完後整個丟掉。**最終 image 裡沒有 Node.js。**

這就是為什麼 production runtime 不需要 Node.js。Dockerfile 裡第一段用 Node.js 把原始碼變成靜態檔案，第二段換成 Nginx image，第一段的所有東西都丟掉了。最後的 image 裡**只有 Nginx + 靜態檔案**。

### 4.2 entrypoint.sh：容器啟動時的準備工作

```bash
#!/bin/sh
set -e

: "${PORT:=8080}"
: "${NGINX_RESOLVER:=127.0.0.11}"
: "${JOB_SERVICE_URL:=http://job-service:8080}"
: "${APPLICANT_SERVICE_URL:=http://applicant-service:8080}"
: "${TRACKED_APPLICANT_SERVICE_URL:=http://tracked-applicant-service:8080}"
: "${INTERVIEW_SERVICE_URL:=http://interview-service:8080}"

export PORT NGINX_RESOLVER JOB_SERVICE_URL APPLICANT_SERVICE_URL TRACKED_APPLICANT_SERVICE_URL INTERVIEW_SERVICE_URL

envsubst '${PORT} ${NGINX_RESOLVER} ${JOB_SERVICE_URL} ${APPLICANT_SERVICE_URL} ${TRACKED_APPLICANT_SERVICE_URL} ${INTERVIEW_SERVICE_URL}' \
  < /etc/nginx/nginx.conf.template \
  > /etc/nginx/nginx.conf

exec nginx -g 'daemon off;'
```

這個腳本做三件事：

#### Step 1：設定預設值

`: "${PORT:=8080}"` 的意思是：如果環境沒給 `PORT`，就用 `8080`。其他變數同理。

這讓你在本地開發時可以直接跑 container，不用設定一堆環境變數。

#### Step 2：把變數塞進設定檔

```bash
envsubst '...' < nginx.conf.template > nginx.conf
```

- `nginx.conf.template` 裡面有 `${PORT}`、`${JOB_SERVICE_URL}` 等佔位符
- `envsubst` 把它們換成實際的值
- 產生真正的 `nginx.conf`

> **沒有這個腳本**：Nginx 啟動時會讀到 `${PORT}` 而不是 `8080`，直接啟動失敗。

#### Step 3：啟動 Nginx

```bash
exec nginx -g 'daemon off;'
```

- `exec`：用 Nginx 取代目前的 shell process，這樣 container 結束時會正確回傳 Nginx 的結束碼
- `daemon off`：Nginx 不要背景執行，保持在前台——這是 Docker container 的標準做法

### 4.3 nginx.conf：請求進來時的處理規則

這個檔案在 container 啟動時被 `envsubst` 處理過，所有 `${...}` 都已經變成實際值。Nginx 啟動後，按照這裡的規則處理每個 HTTP 請求。

```nginx
events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;

    error_log stderr;

    server {
        listen ${PORT};
        server_name localhost;

        root /usr/share/nginx/html;
        index index.html;

        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;

        location / {
            try_files $uri $uri/ /index.html;
            location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
                expires 1y;
                add_header Cache-Control "public, immutable";
            }
            location ~* \.html$ {
                add_header Cache-Control "no-cache, no-store, must-revalidate";
            }
        }

        location /api/jobs {
            proxy_pass ${JOB_SERVICE_URL};
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_hide_header 'Access-Control-Allow-Origin';
            add_header 'Access-Control-Allow-Origin' '*' always;
            proxy_connect_timeout 30s;
            proxy_read_timeout 60s;
            proxy_buffering off;
        }

        # ... 其他 /api/... 設定省略 ...

        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}
```

#### 核心區段解釋

| 區段 | 作用 |
|------|------|
| `listen ${PORT}` | 監聽 Cloud Run 指定的 port（8080） |
| `root /usr/share/nginx/html` | 靜態檔案放在這裡 |
| `try_files $uri $uri/ /index.html` | SPA fallback：找不到檔案時回傳 `index.html`，讓 Angular Router 處理 |
| `location ~* \.(js\|css\|...)` | 靜態資源快取一年（因為檔名有 hash，變更會產生新檔名） |
| `location ~* \.html$` | HTML 不要快取，確保使用者拿到最新版 |
| `location /api/jobs` | 反向代理到後端服務，並處理 CORS |
| `location /health` | 健康檢查 endpoint，Cloud Run 用來確認服務活著 |

---

## 第五章：反向代理有兩層

這個專案用了「反向代理」兩次，但它們在不同層級，負責不同的事。

### 5.1 第一層：Cloud Run（基礎設施層）

當使用者打 `https://你的服務-url.run.app`：

```
使用者瀏覽器 ──HTTPS──▶ Cloud Run 前端（Google 管理的 load balancer）
                              │
                              ▼
                    轉發 HTTP 請求到 container 的 $PORT（8080）
```

這一層 Cloud Run 確實是反向代理——它接收外部 HTTPS，轉發到內部 container。但這是 **Google 管理的基礎設施**，你無法設定它。它只做「把請求送進 container」，不做任何應用層邏輯。

### 5.2 第二層：Nginx（應用層）

請求進到 container 後，Nginx 決定怎麼處理：

```
Nginx 收到請求
    │
    ├── /index.html 或 /main.js ──▶ 直接回傳靜態檔案
    │
    ├── /api/jobs ──▶ proxy_pass 到 http://job-service:8080
    │
    ├── /api/applicants ──▶ proxy_pass 到 http://applicant-service:8080
    │
    └── 其他路徑 ──▶ try_files 回傳 index.html（SPA routing fallback）
```

Nginx 還負責：
- **CORS 處理**：加上 `Access-Control-Allow-Origin` 等 header
- **Gzip 壓縮**：把回傳內容壓縮，減少流量
- **快取控制**：靜態檔案設 `Cache-Control: public, immutable`，HTML 設 `no-cache`
- **SPA fallback**：沒有對應檔案時回傳 `index.html`，讓 Angular Router 處理前端路由

這些是 **應用層邏輯**，Cloud Run 完全不會幫你做。

### 5.3 兩層對照

| 層級 | 誰在做 | 負責什麼 | 你能控制嗎？ |
|------|--------|---------|------------|
| **基礎設施層** | Cloud Run | HTTPS 終止、請求轉發到 container port | ❌ 不能 |
| **應用層** | Nginx | 路徑路由、反向代理 API、CORS、壓縮、快取 | ✅ 可以（透過 nginx.conf） |

如果你把 API 反向代理拿掉，讓前端直接打後端 URL，那理論上可以用更簡單的 HTTP server（例如 Go 寫的 20 行 static file server）來替代 Nginx。但前提是後端要先解決 CORS。

---

## 第六章：替代方案與延伸思考

### 6.1 不用 Nginx 的替代方案

| 方案 | 適合情境 | 優缺點 |
|------|---------|--------|
| **Go/Python 簡易 file server** | 後端已解決 CORS，只想極簡部署 | 映像檔極小，但需要自己寫 routing 邏輯 |
| **Cloud Storage 靜態網站代管** | 純靜態展示，沒有 API 呼叫 | ❌ **不需要 container**，但沒有反向代理能力 |
| **Cloud Run volume mounts + Nginx**（2024 新功能）| 靜態檔案很大、常更新，不想重新 build image | 靜態檔案存在 GCS，Nginx 讀掛載的 bucket |

### 6.2 為什麼這個專案繼續用 Nginx

因為它同時解決了：
1. 靜態檔案服務（`try_files`、`root`）
2. API 反向代理 + CORS（`location /api/... { proxy_pass ... }`）
3. Gzip、快取控制、SPA fallback

一個工具解決五件事，設定也相對標準化，所以維護成本低。

---

## 附錄：常見誤區

**誤區一：「Cloud Run 會自動 serve 我的靜態檔案」**
> 不會。Cloud Run 只負責把 HTTPS 請求轉發到 container port。靜態檔案不會自己變成 HTTP response，你需要 Nginx（或其他 HTTP server）來讀檔案、組 response。

**誤區二：「開發時需要 Node.js，所以 production 也需要」**
> 開發時 Node.js 是「執行環境」和「編譯工具」。但 `ng build` 完成後，產出的是瀏覽器能直接執行的 JavaScript，不需要 Node.js 引擎。Production 的 Node.js 只會浪費資源。

**誤區三：「Cloud Run 有反向代理功能，所以 Nginx 多餘」**
> Cloud Run 做的是「基礎設施層」反向代理（HTTPS → HTTP 轉發）。Nginx 做的是「應用層」反向代理（`/api/jobs` → 後端服務、加 CORS header、SPA fallback）。兩層負責不同的事。

**誤區四：「Dockerfile 裡有 Node.js，所以 container 裡一定有 Node.js」**
> Dockerfile 是多階段 build。第一階段的 `FROM node:18-alpine` 在第二階段 `FROM nginx:alpine` 開始時就整個丟掉了。最終 image 裡只有 Nginx。

---

## 附錄：檔案對照表

| 檔案 | 階段 | 職責 |
|------|------|------|
| `Dockerfile` | Build time | 定義「用 Node.js 編譯 Angular、再用 Nginx 包裝靜態檔案」 |
| `entrypoint.sh` | Container startup | 把環境變數塞進 nginx.conf、啟動 Nginx |
| `nginx.conf` | Runtime | 決定每個 HTTP 請求怎麼處理（靜態檔案、API 反向代理、CORS） |
| `package.json` | Build time | 定義 `ng build` 指令和依賴套件 |
| `dist/my-angular-app/browser/` | Runtime | `ng build` 產出的最終靜態檔案 |

---

*這份筆記對應的程式碼在 `ats-frontend/my-angular-app/` 目錄下。如果你要修改部署行為，請修改 `Dockerfile`、`entrypoint.sh` 或 `nginx.conf`。*